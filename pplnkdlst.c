#include<stdio.h>
#include<stdlib.h>
struct node{
int data;
struct node*next;
}*top==NULL;

void push(int d);
void push(int d)
{
struct node*temp=malloc(size of (struct  node*))
temp->data=d;
temp->next=NULL;
temp->next=top;
top=temp;
}
int display();
int display()
{
printf("%d\n",top->data);
top+topp->next;
}
}
int pop();
int main
{
push(60);
dsplay;
}
